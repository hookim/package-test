{
  "name": "@hookim/package-test",
  "version": "0.0.1",
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release-dev": "npm run changeset version --snapshot dev && npm run changeset publish --no-git-tag-version --tag dev"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
